# Task 019: 관리자 설정

## 상태: 대기

## Phase: 5 - 고도화

## 목표

시스템 설정 페이지 구현. API 키 관리, 기본 템플릿 설정, PIN 기본 인증.

## 구현 사항

- [ ] 설정 페이지 UI (app/settings/page.tsx)
- [ ] API 키 설정/변경 (환경변수 기반)
- [ ] 기본 AI 모델 선택
- [ ] 기본 템플릿 설정
- [ ] PIN 기본 인증 (미들웨어)
- [ ] 설정 API (GET/PUT /api/settings)

## 관련 파일

- `app/settings/page.tsx` (신규)
- `app/api/settings/route.ts` (신규)
- `middleware.ts` (신규, PIN 인증)

## 의존성

- 없음 (독립적)

## 테스트 체크리스트

- [ ] 설정 페이지 접근 확인
- [ ] API 키 변경 → 반영 확인
- [ ] PIN 인증 동작 확인
